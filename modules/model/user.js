function User(id, nickname, mail, pwd, avatar, pos, favs, spreaded, history, settings) {
  this.id = id;
  this.nickname = nickname;
  this.mail = mail;
  this.pwd = pwd;
  this.avatar = avatar;
  this.pos = pos;
  this.pos = favs;
  this.pos = spreaded;
  this.pos = history;
  this.pos = settings;
}
